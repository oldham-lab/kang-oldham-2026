# Reference index for SCZ-associated genes
#
# Maps gene symbol -> bibliography number used as superscripts in the summary table.
# Any gene not listed here displays a positional placeholder (1–10) as a superscript.
#
# To finalize: add entries in the form  GENE = <integer>, e.g.:
#   CNTNAP2 = 1,
#   DLG2    = 2,
#
# Only genes that appear in the table need entries.

scz_ref_index <- setNames(integer(0), character(0))
